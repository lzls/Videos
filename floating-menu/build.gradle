apply plugin: 'com.android.library'

apply from: "$rootDir/common_build.gradle"

android {
    defaultConfig {
        //noinspection MinSdkTooLow
        minSdkVersion 3
        versionCode 1
        versionName "1.0"
    }
}
