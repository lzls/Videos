apply plugin: 'com.android.library'

android {
    lintOptions {
        abortOnError false
    }
    defaultConfig {
        minSdkVersion 9
    }
}

apply from: new File(projectDir, "tools/gradle-on-demand.gradle")
apply from: new File(rootDir, "common_build.gradle")
