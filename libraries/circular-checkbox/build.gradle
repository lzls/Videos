apply plugin: 'com.android.library'

apply from: "$rootDir/common_build.gradle"

android {
    defaultConfig {
        //noinspection MinSdkTooLow
        minSdkVersion 11
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    api project(libraryModulesPrefix + ':common')
}
