apply plugin: 'com.android.library'

apply from: "$rootDir/common_build.gradle"

android {
    defaultConfig {
        //noinspection MinSdkTooLow
        minSdkVersion 11
        versionCode 10
        versionName "2.0.1"
    }
}

dependencies {
    api project(':common')
}
